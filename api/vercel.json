{
  "version": 2,
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  },
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": { "timeout": 60 }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }]
}
